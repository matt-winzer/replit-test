// ------------ FILTER STRUCTURE ------------

// array to be filtered
var numbers = [1, 2, 3, 4, 5];

// filtering and storing result in variable
var filteredNums = numbers.filter(function(number, i, originalArr) {
  // filtering logic...
});
